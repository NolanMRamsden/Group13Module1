/*
 * Level.c
 *
 *  Created on: Sep 25, 2014
 *      Author: Karen
 */
#include "../Headers/Level.h"

void initLevel(int lev)
{
	level->balls = malloc(sizeof(Ball));
	level->paddle = malloc(sizeof(Paddle));
	initPaddle(level->paddle,30);
	startBall(level->balls[0],20,20,300,100);
}
